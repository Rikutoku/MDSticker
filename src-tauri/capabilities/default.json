{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability applied to all MDSticker note windows",
  "windows": ["*"],
  "permissions": ["core:default"]
}
